function lucky(){
     var luckynum=[];
     var count=0;
    for( var i=0;i<=4;i++){
      var random=Math.random()*100;
        random=parseInt(random);
        if()
        luckynum[i]=random;
        console.log(luckynum[i])
    }
